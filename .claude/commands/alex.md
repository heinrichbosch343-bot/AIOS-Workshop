# Alex

> Same command as `/hormozi`: ask Alex Hormozi's books anything, or have them build an offer, money model, lead magnet or script with you.

## Variables

request: $ARGUMENTS

---

Read `.claude/commands/hormozi.md` and follow it exactly, using the `request` above. Keep this file an alias and put every change in `hormozi.md`, so the two commands can never behave differently.
